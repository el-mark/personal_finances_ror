<h1>Nueva transacción Manual</h1>
<%= form_with model: @email, url: { controller: 'index', action: 'email_new' }, local: true do |form| %>
  <%= form.text_area :body, rows: 4, cols: 50 %>
  <br>
  <%= form.submit %>
<% end %>