<h1>Agrega el texto de un correo</h1>
<p>por ejemplo una notificación de transacción, y crea un movimiento en esta cuenta automáticamente a partir del texto</p>
<%= form_with model: @email, url: { controller: 'index', action: 'email_new' }, local: true do |form| %>
  <%= form.text_area :body, rows: 4, cols: 50, class: "form-control" %>
  <br>
  <%= form.submit "Guardar", class: "btn btn-success"  %>
<% end %>